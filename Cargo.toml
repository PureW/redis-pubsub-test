[package]
name = "redis-test"
version = "0.1.0"
authors = ["Anders Bennehag <anders@bennehag.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
redis = { version = "0.15.1", features = [ "cluster"] }
